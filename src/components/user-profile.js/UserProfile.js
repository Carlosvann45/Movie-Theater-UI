import React from 'react';
import classes from './UserProfile.module.css';

const UserProfile = () => (
  <div className={classes.pageWrapper}>You are logged in!</div>
);

export default UserProfile;
